# CSDB Import

CSDB import service.

Runs an embedded SCP and Jetty server that:
 * accepts files over SCP and writes them encrypted to a local directory
 * provides an API for listing and retrieving available files
